export const environment = {
  production: true,
  apiUrl: 'https://141.148.17.246:8082/api' // Default IP for now, user will change DNS later, but actually GitHub Pages requires HTTPS. We must use a domain. Let's use the one from the PDF: https://gilsonsouzaportfolio.duckdns.org/candidato-api
};
